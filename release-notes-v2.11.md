# v2.11 修复

- 修复双指缩放后进入 Viewer 再返回时，首页网格列数恢复默认导致定位错位的问题。
- 首页顶部移除预加载档位显示，只在 Viewer 和调试信息里保留队列/预加载状态。
- 生成页图片 tab 改为版本默认收起；点击展开后图片直接向下铺开，不再限制在小框内滚动。
- 生成页版本参数允许两行显示，避免挤压展开/收起按钮。
- 生成页图片和视频 tab 都增加滚动条与回到顶部按钮。
- 修复视频 VR 输出时间轴：编码帧按原视频 FPS 写入 presentation timestamp，音频复制保持原 sampleTime，避免先播封面+BGM 后播无声视频。
- 视频缓存增加 `encoderV2` 标记，旧问题视频文件不删除，但不再识别为当前 READY 缓存，需要重新生成。
