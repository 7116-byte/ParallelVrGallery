# v2.1 全部页分页与 VR 来源预加载

- 全部页改为统一 `MediaStore.Files` 分页查询，图片和视频按同一时间线混排。
- 全部页接近底部自动预取下一页，不再只停留在最近 1800 项。
- 相册详情继续按当前相册 `bucketId` 分页加载。
- 从全部页打开 Viewer 时，左右滑动和 VR 预加载按全部页当前加载范围执行。
- 从相册详情打开 Viewer 时，左右滑动和 VR 预加载只按当前相册范围执行。
- 关闭 Viewer 后不再触发全局预加载，避免来源范围被清空后错误排队。
