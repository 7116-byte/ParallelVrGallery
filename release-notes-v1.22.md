# v1.22 大图库加载性能修复

- 修复几万张图片/视频时，全部页、相册页、生成页加载后卡住，甚至设置也无法点击的问题。
- 缓存状态改为“扫描已有缓存目录 -> 反查媒体 key”，不再对每张媒体逐个访问文件系统。
- READY 状态只为已生成缓存记录，不再给几万张普通媒体都建立 NORMAL 状态。
- 全部页日期分组改成线性 header/item，减少首次渲染的大列表复制。
- 相册详情不再继承全部页滚动位置，避免大图库滚动下标进入小相册后异常。

这版是 v1.21 的性能修复版，建议大图库用户直接覆盖安装。
