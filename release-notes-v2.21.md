# v2.21

- 修复 v2.20 复用视频深度模型会话后，部分设备生成速度明显变快但 3D 视差消失的问题。
- 深度模型输出改为 direct ByteBuffer，避免复用 GPU Interpreter 时多层 Java Array 输出异常。
- 视频缓存版本升级到 `encoderV3`，隔离 v2.20 可能生成过的错误中间帧和视频缓存。

说明：

- 画质参数没有降低。
- 如果 v2.20 已经生成过无 3D 效果的视频，需要用 v2.21 重新生成；新版本不会复用 v2.20 的坏缓存。
