# v2.24

- 修复图片 VR 深度图可能全黑、导致图片没有 3D 视差的问题：GPU 深度输出范围异常时自动回退到 CPU 重跑。
- 图片缓存版本升级到 depthV2，旧的黑深度图缓存不会继续作为 READY 结果。
- 视频按真实帧数生成时，FPS 优先使用 `帧数 / 原视频时长` 推导，避免帧数增加后时间轴不匹配。
- 视频缓存版本升级到 encoderV6，需要重新生成视频以使用新的时间轴策略。
