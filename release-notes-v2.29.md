# v2.29

- 视频生成从单帧串行改为有界流水线：解码、深度/SBS 生成、编码三个阶段并行推进。
- 每帧仍独立跑深度，不做隔帧复用或插值，避免大幅运动视频引入新的深度错位。
- 默认只缓存生成后的 SBS 帧，不保存原始拆帧，控制磁盘占用。
- 调试页新增流水线状态：解码队列、生成队列、等待编码、缓存命中。
- job.log 每 30 帧记录一次阶段平均耗时，便于判断瓶颈。
- 视频缓存版本升级到 encoderV11，隔离旧串行生成结果。
