# v2.30

- 新增视频深度 worker 数设置，默认 2 个独立模型会话并行跑原始深度推理。
- 视频流水线改为多 depth worker + 按帧序后处理/时序平滑/编码，避免并行破坏深度稳定性。
- 视频缓存版本升级到 `encoderV12`，缓存名包含 `dw1/dw2`，不同 worker 设置互不混用。
- 调试页和日志显示实际 depth worker 数，方便判断视频推理配置。
- 修复 GitHub README 乱码，改为 UTF-8 中文说明。

说明：如果设备显存或内存压力较大，可以在设置中把“视频深度 worker 数”从 2 改回 1。
