# v1.14

- 图片 VR 页单点点击恢复为显示/隐藏信息控件切换。
- 视频“帧生成 xxms”改为最近三帧生成间隔的滚动平均。
- 视频排队或生成中时按钮改为“暂停生成”，暂停后可“继续生成”，方便修改参数做对比。
- 模型选择改为纵向按钮列表，长模型名不会挤成一团。
- 新增 Qualcomm Depth Anything V2 TFLite 实验模型下载入口。

已验证：`assembleDebug` 构建通过。
