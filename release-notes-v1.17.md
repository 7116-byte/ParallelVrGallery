# v1.17

- 生成管理页支持长按多选，批量保存、重新生成、删除图片/视频 VR 缓存。
- 图片重新生成使用当前图片参数；视频重新生成使用当前视频参数。
- 暂停后继续仍沿用暂停时的原参数和原缓存。
- 深度强度、深度平滑、边缘填充、输出最大长边移到公共设置区。
- 设置页移除顶部模型就绪状态，避免回到顶部反复看到“模型已就绪”。
- 设置页调整为公共区、图片区、视频区的层级结构。

已验证：`assembleDebug` 构建通过。
