v2.38

- Moved the Generated page image/video switch into the top bar to prevent the old title and inner tabs from overlapping.
- Changed generated image versions into album-style version tiles with large thumbnails and version names below them.
- Added a second-level generated-version browser for the images inside one selected version.
- Fixed generated-version viewer swiping so left/right browsing stays inside the selected version and uses that version's generated SBS entries.
- Kept v2.37 model download hardening and forced-GPU behavior unchanged.
