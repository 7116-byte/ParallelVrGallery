# v2.0 相册分页与版本调整

- 相册详情改为按 `bucketId` 分页加载，点进相册先加载一页，继续下滑自动加载后续页。
- 相册详情不再受首页最近 1800 项限制，能逐步加载完整相册内容。
- 相册列表继续后台全量聚合，数量显示完整。
- 版本号提升到 `2.0`。

注意：Android 覆盖安装要求 APK 签名一致。版本号改为 2.0 不会绕过签名冲突；如果系统提示签名不一致，需要卸载旧版后安装，或使用旧签名证书重新签名。
