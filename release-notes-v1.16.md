# v1.16

- 设置页按“公共设置 / 图片生成设置 / 视频生成设置”分区。
- 模型状态改为快速存在检查，避免设置页反复 hash 大模型文件；生成前仍校验 SHA-256。
- 视频缓存按生成参数版本分目录，模型、CPU 线程、GPU 开关变化后互不混用。
- 暂停视频后继续生成会沿用暂停时的参数和缓存，不再清理已生成帧。
- 图片和视频缓存版本加入 CPU 线程数与 GPU 开关，修改这些设置后会真正触发重新生成。
- 图片生成日志记录 TFLite 线程数、GPU 请求和 delegate 是否启用。

已验证：`assembleDebug` 构建通过。
