# v1.15

- 图片模型和视频模型拆成独立选择项。
- 旧版本的 `modelId` 会迁移为图片模型和视频模型的默认值。
- 设置页分别显示图片模型和视频模型状态。
- 视频生成优化区域新增视频模型选择，继续保留视频 CPU/GPU 独立设置。
- 暂停中的视频继续生成时会清理该视频拆帧缓存，避免切换视频模型后混用旧帧。
- 视频参数文件记录实际使用的 `depthModel`。

已验证：`assembleDebug` 构建通过。
