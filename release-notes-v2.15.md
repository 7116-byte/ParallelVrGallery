# v2.15 修复

- 修复点击刷新后生成中的视频从生成页消失的问题：刷新媒体库会保留 `QUEUED / GENERATING / PAUSED / FAILED` 视频状态和任务。
- 如果生成中的视频不在本次最近媒体列表内，会作为生成页虚拟项保留，不污染全部页。
- 相册主页字体缩小改为 6/7/8 列分别减 2/4/6 级。
- 普通图库缩略图角标（原图、未生成、生成中等）在 6/7/8 列时同步减 2/4/6 级。
